[package]
name = "corki-mcp-regex"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
regex-lite = "=0.1.8"
regex = "=1.12.3"

[profile.release]
opt-level = "s"
lto = true
strip = true
panic = "abort"
