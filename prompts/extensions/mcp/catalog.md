<mcp_instructions>
## MCP tools

The following inventory comes from configured Model Context Protocol servers. Direct tools are callable now; deferred sources require tool_search to load their definitions before use. Preserve the tool's full name. MCP resources and resource templates are not a tool discovery API.

#{tools}

Startup warnings:
#{warnings}
</mcp_instructions>
