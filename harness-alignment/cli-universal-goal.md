# CLI 通用交互对齐目标（2026-09-23）

以本轮用户完整 goal 为约束。Corki：`/Users/corki/PycharmProjects/corki`；
参考源码：`/Users/corki/IdeaProjects/ad/codex`。

## 独立完成清单

- [x] 1 长文本粘贴折叠：阈值、占位符、完整展开、paste-burst、防误提交，编辑/删除/撤销/排队/历史恢复。（清理顺序、缓存边界、超长输入差异及单元/PTY 验收见 progress。）
- [x] 2 文件、技能/插件引用候选：搜索、键盘选择、确认取消与真实运行时绑定，不引入官方服务目录。（本地原生搜索、默认安装/wheel、运行时绑定与PTY证据见progress；平台及无helper降级差异明确保留。）
- [x] 3 同次运行输入历史恢复图片、真实标记、位置与长粘贴；区分纯文本跨进程历史、队列和会话。（近期输入缓存容量及跨进程边界见 progress；不删除旧磁盘历史。）
- [x] 4 图片路径粘贴识别：中文、空格、引号、无效/非图片/超限，兼容普通文本及 Ctrl+V。（源码/真实文件解码/运行时/PTY 验收见 progress；未访问真实用户剪贴板，跨平台真实终端限制详见记录。）
- [x] 5 工具执行归类聚合：参考 Exploring/Explored，保留顺序、错误、完整历史及展开，不改变执行语义。（调用边界/失败状态、完整输出链路、有界历史存储及资源回收均有单元/集成/PTY证据；必要分类及容量差异见 progress。）
- [x] 6 本地会话选择：列表、搜索、目录筛选、预览、恢复、归档/取消归档，兼容最近/指定 ID，失败与取消安全。（源码/目录恢复/归档/资源回收及 PTY 证据见 progress。）
- [x] 7 回溯旧问题：参考 Esc 选择/确认，在新分支恢复草稿及图片/引用/上下文，原会话不变；不重跑旧工具或回滚文件。（分页显示及样式差异、源码/运行时/PTY 证据见 progress。）
- [x] 9 终端通知：完成/需要操作触发、焦点判断、配置、OSC 9/BEL 与安全降级，不重复或泄露敏感内容。（源码/应用链路/PTY 验收见 progress；真实桌面弹窗受终端设置影响，未人工验证。）

## 实施规则

每项先阅读 Codex 对应源码、调用链、测试/快照，再检查 Corki 并实施。
记录参考文件/函数、交互规则、差距、实现、必要差异和验收结果。
默认行为、按键、确认取消、文案布局和状态转换尽可能一致，不机械移植 Rust。
正常范围自行推进；新权限、破坏性操作或明显扩围才询问用户。
优先处理共享输入状态的 1/3/4，再完成其余项目；不得以部分完成代替整个目标完成。

## 排除与保护

不做 8 外部编辑器、10 可配置状态栏、11 Vim/自定义快捷键。
不新增官方登录/账户/额度/订阅/云端依赖，不恢复原生 tool search、远程压缩 namespace。
供应商兼容协议仍作为普通供应商；不扩展无关斜杠命令或重启全面 harness 核心对齐。
保护现有用户改动，不修改 teach.md，不自动提交推送。
未经另行授权不读取真实用户剪贴板、密钥或调用付费模型。
此前 Shift+Enter 目标已完成，证据保留于 cli-keyboard-review.md，不回退其成果。

## 验收门槛

8 项各自完成端到端功能及源码对照记录，全部通过相关回归才结束 goal。
覆盖正常、取消、失败、重复操作、中文多行、窄宽屏/resize、异步过期、执行中输入、审批、队列、恢复和退出。
异步任务/进程/监听器/定时器/图片/缓存有界且可取消回收，退出恢复终端。
增加单元、集成、真实 PTY 测试，回归图片、Shift+Enter、Esc、模型菜单、审批、流式输出与历史。
独立测试合理并行，最多 96 workers；按内存/PTY/FD 及共享资源选择实际并发，有冲突须隔离或串行。
验收报告必须有实际退出结果，不以已启动或部分通过代替完成。
最终列完成清单、使用说明、必要差异、测试结果和未验证的真实终端/供应商限制。

## 验收结论

八项已完成。最终相关联合回归1233 passed，隔离原生搜索9 passed，全E2E400 passed。
首次全项目回归失败及后续修复/隔离批次如实记录，不将条件跳过或首次失败计作通过。
详见 `cli-universal-acceptance.md` 与 `cli-universal-progress.md`。
